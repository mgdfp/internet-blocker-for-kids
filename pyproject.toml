[project]
name = "internet-blocker-for-kids"
version = "0.1.0"
description = "Daily screen time enforcer for UniFi UDM Pro"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.32.5",
]
